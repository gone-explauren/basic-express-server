'use strict';

const getName = require('./validator.js');

describe('Testing the getName function', () => {
	test('Should log req.method and req.path then pass to the next middleware', () => {
expect(true).toEqual(true);
	})
})